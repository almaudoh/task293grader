# ENDPOINT_PREFIX: /api
REQUIRED_ENDPOINTS:
  query:
    path: /prompt
    props:
      - query
    mime_type: multipart/form-data
  upload:
    path: /upload
    props:
      - files
  health:
    path: /health
    props: []
